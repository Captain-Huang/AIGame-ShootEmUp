namespace AIGame.ShootEmUp.Core
{
    public enum GameState
    {
        Boot,
        MainMenu,
        Playing,
        Paused,
        LevelCleared,
        GameOver,
        GameCompleted
    }
}
